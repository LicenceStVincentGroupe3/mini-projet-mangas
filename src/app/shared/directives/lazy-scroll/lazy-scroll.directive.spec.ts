import { LazyScrollDirective } from './lazy-scroll.directive';

describe('LazyScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyScrollDirective();
    expect(directive).toBeTruthy();
  });
});
